import { AdminUserRolRequest } from './admin-user-rol-request.model';

describe('AdminUserRolRequest', () => {
  it('should create an instance', () => {
    expect(new AdminUserRolRequest()).toBeTruthy();
  });
});
