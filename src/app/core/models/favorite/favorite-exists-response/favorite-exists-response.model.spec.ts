import { FavoriteExistsResponse } from './favorite-exists-response.model';

describe('FavoriteExistsResponse', () => {
  it('should create an instance', () => {
    expect(new FavoriteExistsResponse()).toBeTruthy();
  });
});
